{
  "title": "Publication config",
  "type": "object",
  "required": [
    "artifacts",
    "client",
    "server",
    "curseforge_enabled",
    "modrinth_enabled"
  ],
  "properties": {
    "client": {
      "type": "boolean",
      "description": "Indicates if the mod is client-side",
      "title": "Client side",
      "default": false
    },
    "server": {
      "type": "boolean",
      "description": "Indicates if the mod is server-side",
      "title": "Server side",
      "default": false
    },
    "curseforge_enabled": {
      "type": "boolean",
      "description": "Whether the artifact(s) should be published to Curseforge",
      "title": "Deploy to Curseforge",
      "default": false
    },
    "modrinth_enabled": {
      "type": "boolean",
      "description": "Whether the artifact(s) should be published to Modrinth",
      "title": "Deploy to Modrinth",
      "default": false
    },
    "discord_enabled": {
      "type": "boolean",
      "description": "Whether the artifact(s) should be published to Discord",
      "title": "Announce on Discord",
      "default": false
    },
    "curseforge_project_slug": {
      "type": "string",
      "description": "The Curseforge project slug",
      "title": "Curseforge project slug",
      "pattern": "\\S"
    },
    "curseforge_project_id": {
      "type": "string",
      "description": "The Curseforge project ID",
      "title": "Curseforge project ID",
      "pattern": "\\d+"
    },
    "modrinth_project_id": {
      "type": "string",
      "description": "The Modrinth project ID",
      "title": "Modrinth project ID"
    },
    "discord_webhook": {
      "type": "object",
      "title": "Discord Webhook",
      "properties": {
        "discord_branch": {
          "type": "string",
          "description": "Updates from which **single** branch should be published to discord. What branch contains the **\"main\"** version of the mod? Enter the **exact** branch name.",
          "title": "Discord branch",
          "pattern": "\\S"
        },
        "username": {
          "type": "string",
          "description": "Username used to send the webhook, defaults to `Mod Publish Plugin`",
          "title": "Username"
        },
        "avatar_url": {
          "type": "string",
          "title": "Avatar URL",
          "description": "Avatar image URL for the webhook, defaults to `none`"
        },
        "content": {
          "type": "string",
          "title": "Content message",
          "description": "Set the content message, changelog is **always appended** after this content. Use `{version}` as a placeholder for the released version. Defaults to \n`v{version} was released!`"
        },
        "thumbnail_url": {
          "type": "string",
          "title": "Thumbnail URL",
          "description": "Image URL for the discord embeds thumbnail, e.g. Mod's logo"
        },
        "embed_color": {
          "type": "string",
          "title": "Embed color",
          "description": "Color of the left line used in the discord embed. Full `RRGGBB` hex color prefixed with a `#`.",
          "pattern": "^#[0-9a-fA-F]{6}$"
        }
      },
      "additionalProperties": false,
      "required": [
        "discord_branch"
      ]
    },
    "artifacts": {
      "title": "Artifacts / Branches",
      "type": "array",
      "description": "Configuration for artifacts that should be released",
      "minItems": 1,
      "items": {
        "title": "Artifact / Branch",
        "type": "object",
        "properties": {
          "file_glob": {
            "title": "File glob",
            "type": "string",
            "description": "The glob for the compiled artifact to be uploaded. Can use `{version}` which will be replaced with the current mod version. E.g. \n`build/libs/*-1.21.1-neoforge-{version}.jar`",
            "pattern": "\\S"
          },
          "branch": {
            "title": "Git Branch",
            "type": "string",
            "description": "The name of the branch from which the artifact is compiled",
            "pattern": "\\S"
          },
          "loaders": {
            "title": "Mod loaders",
            "type": "array",
            "description": "Mod loaders that can load the artifact",
            "minItems": 1,
            "items": {
              "type": "string",
              "enum": [
                "neoforge",
                "fabric",
                "forge"
              ]
            },
            "uniqueItems": true
          },
          "game_versions": {
            "title": "Game versions",
            "type": "array",
            "description": "Supported game versions for the artifact (e.g. `1.21.1`)",
            "minItems": 1,
            "items": {
              "title": "Game Version",
              "pattern": "^\\d+(\\.\\d+)+$",
              "type": "string",
              "minLength": 1
            },
            "uniqueItems": true
          },
          "java_version": {
            "title": "Java version",
            "type": "string",
            "description": "The java version required to build the branch and run the mod (e.g. `17`)",
            "pattern": "\\S"
          },
          "version_properties": {
            "title": "File version replacements",
            "type": "array",
            "description": "List of files and respective properties where the version should be set. Only supports properties format (`property=value`). Use your gradle build script to distribute the value to other files. If none is supplied, defaults to `mod_version` in `gradle.properties`.",
            "items": {
              "title": "Property file",
              "type": "object",
              "properties": {
                "file": {
                  "title": "File path",
                  "type": "string",
                  "description": "The exact relative path to the file (e.g. `gradle.properties`)",
                  "pattern": "\\S"
                },
                "property": {
                  "title": "Property name",
                  "type": "string",
                  "description": "The name of the property which should be set to the version (e.g. `mod_version`)",
                  "pattern": "\\S"
                }
              },
              "required": [
                "file",
                "property"
              ],
              "additionalProperties": false
            }
          },
          "dependencies": {
            "type": "object",
            "description": "Dependencies for the artifact (merged with **common dependencies**)",
            "$ref": "#/$defs/dependencies"
          }
        },
        "additionalProperties": false,
        "required": [
          "branch",
          "loaders",
          "game_versions",
          "java_version",
          "file_glob"
        ]
      },
      "uniqueItems": true
    },
    "common_dependencies": {
      "title": "Common dependencies",
      "type": "object",
      "description": "Common dependencies for **each artifact** (all branches)",
      "$ref": "#/$defs/dependencies"
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "curseforge_enabled": {
            "const": true
          }
        }
      },
      "then": {
        "required": [
          "curseforge_project_slug",
          "curseforge_project_id"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "modrinth_enabled": {
            "const": true
          }
        }
      },
      "then": {
        "required": [
          "modrinth_project_id"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "discord_enabled": {
            "const": true
          }
        }
      },
      "then": {
        "required": [
          "discord_webhook"
        ]
      }
    }
  ],
  "$defs": {
    "dependencies": {
      "title": "Dependencies",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "curseforge": {
          "title": "Curseforge dependencies",
          "type": "array",
          "description": "Dependencies for Curseforge deployment",
          "items": {
            "$ref": "#/$defs/cf_dependency"
          }
        },
        "modrinth": {
          "title": "Modrinth dependencies",
          "type": "array",
          "description": "Dependencies for Modrinth deployment",
          "items": {
            "$ref": "#/$defs/mr_dependency"
          }
        }
      }
    },
    "dependency_type": {
      "title": "Dependency type",
      "type": "string",
      "description": "Type of the dependency",
      "enum": [
        "required",
        "optional",
        "incompatible",
        "embedded"
      ]
    },
    "cf_dependency": {
      "title": "Curseforge dependency",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "slug": {
          "title": "Slug",
          "type": "string",
          "description": "Curseforge project slug",
          "pattern": "\\S"
        },
        "dependency_type": {
          "type": "string",
          "$ref": "#/$defs/dependency_type"
        }
      },
      "required": [
        "slug",
        "dependency_type"
      ]
    },
    "mr_dependency": {
      "title": "Modrinth dependency",
      "type": "object",
      "additionalProperties": false,
      "description": "Dependency on Modrinth platform. Only one, either ID or slug can be provided, you need to pick which value should be required. If you provide both values, ID takes priority and slug is not used.",
      "properties": {
        "id": {
          "title": "ID",
          "type": "number",
          "description": "ID of the modrinth project"
        },
        "slug": {
          "title": "Slug",
          "type": "string",
          "description": "Slug of the modrinth project",
          "pattern": "\\S"
        },
        "dependency_type": {
          "type": "string",
          "$ref": "#/$defs/dependency_type"
        }
      },
      "oneOf": [
        {
          "title": "Required ID",
          "type": "object",
          "required": ["id"]
        },
        {
          "title": "Required Slug",
          "type": "object",
          "required": ["slug"]
        }
      ]
    },
    "required": [
      "identifier",
      "dependency_type"
    ]
  }
}