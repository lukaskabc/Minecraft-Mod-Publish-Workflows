{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "artifacts"
  ],
  "properties": {
    "client": {
      "type": "boolean",
      "description": "Indicates if the mod is client-side"
    },
    "server": {
      "type": "boolean",
      "description": "Indicates if the mod is server-side"
    },
    "curseforge_project_slug": {
      "type": "string",
      "description": "The Curseforge project slug"
    },
    "curseforge_project_id": {
      "type": "number",
      "description": "The Curseforge project ID"
    },
    "modrinth_project_id": {
      "type": "number",
      "description": "The Modrinth project ID"
    },
    "artifacts": {
      "type": "array",
      "description": "Configuration for artifacts that should be released",
      "minItems": 1,
      "items": {
        "type": "object",
        "properties": {
          "file_glob": {
            "type": "string",
            "description": "The glob for the compiled artifact to be uploaded. Can use {version} which will be replaced with the current mod version. E.g. -1.21.1-neoforge-{version}.jar"
          },
          "branch": {
            "type": "string",
            "description": "The name of the branch from which the artifact is compiled"
          },
          "loaders": {
            "type": "array",
            "description": "Mod loaders that can load the artifact (neoforge, forge, fabric)",
            "minItems": 1,
            "items": {
              "type": "string",
              "enum": [
                "neoforge",
                "fabric",
                "forge"
              ]
            },
            "uniqueItems": true
          },
          "game_versions": {
            "type": "array",
            "description": "Supported game versions for the artifact",
            "minItems": 1,
            "items": {
              "pattern": "^\\d+(\\.\\d+)+$",
              "type": "string"
            },
            "uniqueItems": true
          },
          "java_version": {
            "type": "string",
            "description": "The java version used to run the gradle build task"
          },
          "version_properties": {
            "type": "array",
            "description": "List of files and respective properties where the version should be set (default: mod_version in gradle.properties)",
            "items": {
              "type": "object",
              "properties": {
                "file": {
                  "type": "string",
                  "description": "The relative path to the file"
                },
                "property": {
                  "type": "string",
                  "description": "The name of the property which should be set to the version"
                }
              },
              "required": [
                "file",
                "property"
              ],
              "additionalProperties": false
            }
          },
          "dependencies": {
            "type": "object",
            "description": "Dependencies for the artifact (merged with common dependencies)",
            "$ref": "#/$defs/dependencies"
          }
        },
        "additionalProperties": false,
        "required": [
          "branch",
          "loaders",
          "game_versions",
          "java_version",
          "file_glob"
        ]
      },
      "uniqueItems": true
    },
    "common_dependencies": {
      "type": "object",
      "description": "Common dependencies for each artifact",
      "$ref": "#/$defs/dependencies"
    }
  },
  "$defs": {
    "dependencies": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "curseforge": {
          "type": "array",
          "description": "Dependencies for Curseforge deployment",
          "items": {
            "$ref": "#/$defs/cf_dependency"
          }
        },
        "modrinth": {
          "type": "array",
          "description": "Common dependencies for all artifacts on Curseforge",
          "items": {
            "$ref": "#/$defs/mr_dependency"
          }
        }
      }
    },
    "dependency_type": {
      "type": "string",
      "description": "Type of the dependency",
      "enum": [
        "required",
        "optional",
        "incompatible",
        "embeds"
      ]
    },
    "cf_dependency": {
      "type": "object",
      "description": "Dependency for curseforge",
      "additionalProperties": false,
      "properties": {
        "slug": {
          "type": "string",
          "description": "Curseforge project slug"
        },
        "dependency_type": {
          "type": "string",
          "$ref": "#/$defs/dependency_type"
        }
      },
      "required": [
        "slug",
        "dependency_type"
      ]
    },
    "mr_dependency": {
      "type": "object",
      "description": "Dependency for Modrinth",
      "additionalProperties": false,
      "properties": {
        "dependency_type": {
          "type": "string",
          "$ref": "#/$defs/dependency_type"
        }
      },
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "id": {
              "type": "number",
              "description": "ID of the modrinth project"
            }
          },
          "required": ["id"]
        },
        {
          "type": "object",
          "properties": {
            "slug": {
              "type": "string",
              "description": "Slug of the modrinth project"
            }
          },
          "required": ["slug"]
        }
      ]
    },
    "required": [
      "identifier",
      "dependency_type"
    ]
  }
}